import React from 'react'
import useAuth from '../../hooks/useAuth'

function Register() {
    // const {user}=useAuth()
    return (
        <div>
            
        </div>
    )
}

export default Register
